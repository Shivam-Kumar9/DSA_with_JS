// find all dublcates in an array

let ar = [4, 3, 2, 7, 8, 2, 3, 1];

let i=0;
 
while(i<ar.length){
    let rightIndex = i+1
    if(ar[i] != ar[rightIndex] && rightIndex < ar.length ){
        
    }
}
